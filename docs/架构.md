# acat-book-crawler 架构

> Node.js 书籍爬虫工具 — Puppeteer + Cheerio + Express

## 技术栈

| 层级 | 技术 |
|------|------|
| 运行时 | Node.js ≥ 18 (ES Module) |
| 后端框架 | Express 4.x |
| 前端 | React 19 + TypeScript + Vite 8 |
| HTML 解析 | Cheerio |
| 浏览器自动化 | Puppeteer |
| HTTP 客户端 | Axios + lossless-json |

## 项目结构

```
acat-book-crawler/
├── src/
│   ├── server.js              # Express 入口
│   ├── config.js              # 全量配置（环境变量）
│   ├── services/
│   │   ├── browser.js         # 共享 Puppeteer 浏览器实例
│   │   ├── crawler.js         # 爬虫核心：搜索→爬取→解析
│   │   └── api-client.js      # REST API 客户端（登录/token/401 重试）
│   ├── store/
│   │   ├── storage.js         # 数据持久化（通过 REST API）
│   │   └── crawl-tracker.js   # 本地爬取状态追踪
│   ├── sites/
│   │   ├── SiteAdapter.js     # 站点适配器基类
│   │   ├── registry.js        # 适配器注册表
│   │   ├── site-69shuba.js    # 69shuba 适配器
│   │   └── site-alicesw.js    # alicesw 适配器
│   └── routes/
│       └── crawler.js         # API 路由
├── frontend/                  # React SPA 前端
├── Dockerfile
└── package.json
```

## 架构图

```
Browser (React SPA :5175)
  → /api/crawler/* (Vite proxy → Express :8005)
    → routes/crawler.js (参数校验)
      → services/crawler.js (CrawlerService)
        ├── services/browser.js (共享 Puppeteer)
        └── store/storage.js (数据持久化)
              ├── services/api-client.js (REST API — 数据操作 + 状态上报)
              └── store/crawl-tracker.js (本地追踪)
```

## 爬取流程

1. 搜索书籍（DuckDuckGo/站内搜索）
2. Puppeteer 获取书籍详情页 → Cheerio 解析元数据
3. 通过 crawler API 查找已有书籍 → 不存在则创建
4. 获取目录页 → 解析章节列表
5. 逐章爬取内容 → 通过 API 创建章节+段落
6. 通过 API 上报爬取状态

## 多站点适配

通过 `SiteAdapter` 基类实现适配器模式，每个站点继承并实现：
- URL 模式（书籍/章节/目录 URL 解析）
- HTML 解析规则（元数据/目录/正文提取）
- 搜索策略（站内搜索或外部搜索引擎）
- Puppeteer 交互（Cloudflare 绕过、懒加载内容展开）
